---
name: pigeon
description: Read and search locally-stored messaging data (WhatsApp, Slack, etc.). Use when the user asks about messages, chats, conversations, or wants to find something someone said.
argument-hint: <command> [options]
---

# pigeon — Messaging Data Reader

The pigeon binary is at: __PIGEON_PATH__

Before doing anything else, run `__PIGEON_PATH__ help` to discover available commands and their options.

## Task

$ARGUMENTS

Use `pigeon` to accomplish the task. Follow the help output precisely. Start with `pigeon list` to discover available platforms and accounts, then drill down with `pigeon read` or `pigeon search`.
