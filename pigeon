#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
commit=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
go build -ldflags "-X main.version=dev-${commit}" -o pigeon.bin ./cmd/pigeon
exec ./pigeon.bin "$@"
