Run `./cmu <command>` — the wrapper script auto-builds to `cmu.bin` (gitignored) and executes.
Do not run `go build -o cmu` directly; that overwrites the wrapper script.
`./cmu help` gives everything you need to know about available commands and usage.
